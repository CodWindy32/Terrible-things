# Полная инструкция по настройке главного меню

## 📋 Обзор системы

Система состоит из трех сцен:
1. **MainMenu** - главное меню с настройками
2. **VideoIntro** - видео-заставка
3. **SampleScene** - игровая сцена

## 🎮 Шаг 1: Создание MainMenu сцены

### 1.1 Создайте новую сцену

1. `File → New Scene`
2. Выберите **Basic (Built-in)** или **Basic (URP)**
3. Сохраните как `MainMenu.unity` в папке `Assets/Scenes/`

### 1.2 Настройте камеру

1. Выберите **Main Camera** в Hierarchy
2. Настройте позицию и направление камеры на вашу будущую анимированную сцену
3. Это будет фоновая камера, которая будет размыта

### 1.3 Создайте фоновую сцену (ваша задача)

Создайте в сцене объекты с анимацией:
- Это может быть что угодно: вращающиеся объекты, частицы, анимированные модели
- Камера должна быть направлена на эту сцену
- Всё это будет размыто для элитного вида

### 1.4 Создайте GameObject для UI

1. Создайте пустой GameObject: `GameObject → Create Empty`
2. Назовите его `MainMenuUI`
3. Добавьте компоненты:

#### UI Document:
- `Add Component → UI Toolkit → UI Document`
- **Source Asset**: `Assets/UI_Toolkit/MainMenu/MainMenu.uxml`
- **Panel Settings**: Runtime Panel Settings (создайте если нет)
- **Sort Order**: 0

#### Volume (для blur):
- `Add Component → Volume`
- **Is Global**: ✅
- **Profile**: Нажмите **New**
- **Weight**: 0
- **Priority**: 1

#### Добавьте Depth of Field:
- В Volume нажмите **Add Override**
- Найдите **Depth of Field**
- Настройте:
  - Mode: **Bokeh** ✅
  - Focus Distance: **0.1** ✅
  - Aperture: **0** ✅
  - Focal Length: **50** ✅

#### BlurEffect:
- `Add Component → BlurEffect`
- **Transition Speed**: 20
- **Max Aperture**: 10

#### MainMenuController:
- `Add Component → MainMenuController`
- **Blur Effect**: Перетащите компонент BlurEffect
- **Video Intro Scene Name**: "VideoIntro"

### 1.5 Настройте освещение (опционально)

Добавьте освещение для вашей фоновой анимированной сцены.

---

## 🎬 Шаг 2: Создание VideoIntro сцены

### 2.1 Создайте новую сцену

1. `File → New Scene`
2. Выберите **Basic (Built-in)** или **Basic (URP)**
3. Сохраните как `VideoIntro.unity` в папке `Assets/Scenes/`

### 2.2 Настройте камеру для видео

1. Выберите **Main Camera**
2. Настройки по умолчанию подойдут
3. Камера будет использоваться для рендеринга видео

### 2.3 Создайте GameObject для Video Player

1. Создайте пустой GameObject: `GameObject → Create Empty`
2. Назовите его `VideoPlayer`
3. Добавьте компоненты:

#### Video Player:
- `Add Component → Video → Video Player`
- **Source**: Video Clip
- **Video Clip**: Перетащите ваш MP4 файл из `Assets/Videos/`
- **Render Mode**: Camera Far Plane
- **Camera**: Перетащите Main Camera
- **Play On Awake**: false (управляется кодом)
- **Loop**: false
- **Skip On Drop**: ✅

#### VideoIntroController:
- `Add Component → VideoIntroController`
- **Intro Video**: Перетащите ваш MP4 файл
- **Game Scene Name**: "SampleScene"
- **Allow Skip**: ✅
- **Skip Delay**: 1 (секунда перед возможностью пропустить)

### 2.4 Добавьте ваше видео

1. Скопируйте ваш MP4 файл в папку `Assets/Videos/`
2. Unity автоматически импортирует его
3. Назначьте его в Video Player и VideoIntroController

---

## 🎯 Шаг 3: Build Settings

### 3.1 Добавьте сцены

1. `File → Build Settings`
2. Нажмите **Add Open Scenes** или перетащите сцены:
   - **Index 0**: MainMenu
   - **Index 1**: VideoIntro
   - **Index 2**: SampleScene (ваша игровая сцена)

**Важно:** Порядок имеет значение! MainMenu должно быть первым.

### 3.2 Установите стартовую сцену

1. В Build Settings убедитесь, что MainMenu на первом месте (Index 0)
2. При запуске билда игра начнется с главного меню

---

## 🧪 Шаг 4: Тестирование

### 4.1 Тест главного меню

1. Откройте сцену **MainMenu**
2. Нажмите **Play**
3. Проверьте:
   - ✅ Фон размыт
   - ✅ Название игры отображается
   - ✅ Три кнопки работают
   - ✅ Кнопка "Настройки" открывает панель
   - ✅ Все настройки работают и сохраняются
   - ✅ Кнопка "Назад" возвращает в главное меню

### 4.2 Тест видео-заставки

1. Откройте сцену **VideoIntro**
2. Нажмите **Play**
3. Проверьте:
   - ✅ Видео начинает воспроизводиться
   - ✅ Можно пропустить клавишей (после 1 секунды)
   - ✅ После окончания переходит в игру

### 4.3 Тест полного цикла

1. Откройте сцену **MainMenu**
2. Нажмите **Play**
3. Нажмите "Начать игру"
4. Видео должно запуститься
5. Пропустите или дождитесь окончания
6. Должна загрузиться игровая сцена

---

## 🎨 Настройка внешнего вида

### Изменить название игры

В `MainMenu.uxml` найдите:
```xml
<ui:Label text="TERRIBLE THINGS" ... />
```

Измените на название вашей игры.

### Изменить стили

Откройте `MainMenu.uss` и настройте:

**Размер заголовка:**
```css
.game-title {
    font-size: 96px;  /* Измените по вкусу */
    letter-spacing: 20px;
}
```

**Интенсивность blur:**
В BlurEffect компоненте измените **Max Aperture** (5-20).

---

## 📁 Структура файлов

После настройки у вас будет:

```
Assets/
├── Scenes/
│   ├── MainMenu.unity          ✅ Создано
│   ├── VideoIntro.unity        ✅ Создано
│   └── SampleScene.unity       ✅ Существует
├── UI_Toolkit/
│   └── MainMenu/
│       ├── MainMenu.uxml       ✅ Создано
│       ├── MainMenu.uss        ✅ Создано
│       ├── MainMenuController.cs ✅ Создано
│       └── VideoIntroController.cs ✅ Создано
├── Scripts/
│   └── Managers/
│       └── SceneTransitionManager.cs ✅ Создано
└── Videos/
    └── intro.mp4               📁 Поместите ваше видео сюда
```

---

## 💾 Сохранение настроек

Все настройки автоматически сохраняются в PlayerPrefs:

- `Settings_Quality` - качество графики
- `Settings_Resolution` - разрешение
- `Settings_Fullscreen` - полноэкранный режим
- `Settings_VSync` - вертикальная синхронизация
- `Settings_MasterVolume` - общая громкость
- `Settings_MusicVolume` - громкость музыки
- `Settings_SfxVolume` - громкость эффектов
- `Settings_MouseSensitivity` - чувствительность мыши
- `Settings_InvertY` - инверсия Y-оси
- `Settings_CameraShake` - интенсивность тряски камеры

### Использование настроек в игре

В любом скрипте можете получить настройки:

```csharp
void Start()
{
    float mouseSensitivity = PlayerPrefs.GetFloat("Settings_MouseSensitivity", 50f);
    bool invertY = PlayerPrefs.GetInt("Settings_InvertY", 0) == 1;
    float cameraShake = PlayerPrefs.GetFloat("Settings_CameraShake", 1f);
    
    // Применить к вашим системам
}
```

---

## 🔧 Дополнительные настройки

### Добавить фоновую музыку в главное меню

1. Создайте GameObject `BackgroundMusic`
2. Добавьте `Audio Source`
3. Назначьте музыкальный клип
4. **Loop**: ✅
5. **Play On Awake**: ✅
6. **Volume**: Свяжите с настройками музыки

### Добавить звуки кнопок

В MainMenuController добавьте:

```csharp
[SerializeField] private AudioClip buttonClickSound;
private AudioSource audioSource;

void Awake()
{
    audioSource = gameObject.AddComponent<AudioSource>();
}

void RegisterCallbacks()
{
    startGameButton.clicked += () => {
        PlaySound(buttonClickSound);
        OnStartGameClicked();
    };
}

void PlaySound(AudioClip clip)
{
    if (audioSource != null && clip != null)
        audioSource.PlayOneShot(clip);
}
```

---

## ✅ Чек-лист

### MainMenu сцена:
- [ ] Сцена создана и сохранена
- [ ] Камера настроена
- [ ] Фоновая анимированная сцена создана (ваша задача)
- [ ] GameObject MainMenuUI создан
- [ ] UI Document настроен (MainMenu.uxml)
- [ ] Volume с Depth of Field настроен
- [ ] BlurEffect добавлен
- [ ] MainMenuController добавлен и настроен

### VideoIntro сцена:
- [ ] Сцена создана и сохранена
- [ ] GameObject VideoPlayer создан
- [ ] Video Player компонент настроен
- [ ] VideoIntroController добавлен
- [ ] MP4 видео добавлено в проект
- [ ] Видео назначено в компонентах

### Build Settings:
- [ ] Все три сцены добавлены
- [ ] MainMenu на первом месте (Index 0)
- [ ] Порядок: MainMenu → VideoIntro → SampleScene

### Тестирование:
- [ ] Главное меню открывается с размытым фоном
- [ ] Все кнопки работают
- [ ] Настройки открываются и сохраняются
- [ ] "Начать игру" запускает видео
- [ ] Видео можно пропустить
- [ ] После видео загружается игра

---

## 🎉 Готово!

После выполнения всех шагов у вас будет полноценное главное меню с:
- Элитным черно-белым дизайном
- Размытым анимированным фоном
- Полными настройками (графика, звук, управление)
- Видео-заставкой с возможностью пропуска
- Плавными переходами между сценами
